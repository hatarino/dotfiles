" Show if insert mode or not.
set showmode

" Do not hilighting when searching.
set nohlsearch

" Auto code indent when CR.
set autoindent

" Tab to 4 whitespace.
set tabstop=4
set expandtab

" Set hilighting.
syntax on

" Abbreviations.
abbr _sh #!/bin/bash

